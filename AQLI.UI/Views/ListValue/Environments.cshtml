@model IEnumerable<AQLI.Data.Models.EnvironmentModel>

@{
    ViewData["Title"] = "Environments";
}

<h1>Environments</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-bordered table-striped table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EnvironmentName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EnvironmentName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EnvironmentID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EnvironmentID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EnvironmentID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
