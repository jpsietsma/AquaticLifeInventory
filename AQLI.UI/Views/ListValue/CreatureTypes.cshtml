@model IEnumerable<AQLI.Data.Models.CreatureTypeModel>

@{
    ViewData["Title"] = "CreatureTypes";
}

<h1>CreatureTypes</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-bordered table-striped table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CreatureTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CreatureTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CreatureTypeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CreatureTypeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CreatureTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
